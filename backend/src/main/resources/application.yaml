# ──────────────────────────────────────────────
# A2UI Multi-Agent System — Configuration
# ──────────────────────────────────────────────
# Surcharge via variables d'environnement : ${ENV_VAR:valeur_par_defaut}

intent:
  url: ${INTENT_URL:http://localhost:9998/intent}

generator:
  url: ${GENERATOR_URL:http://localhost:9999/generator}
